<hibernate-configuration xmlns="http://www.hibernate.org/xsd/orm/cfg">
    <session-factory>
    
    	<!-- Database Connection -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/student</property>
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">xyz</property>
        
        <!-- Hibernate Settings -->
        <property name="hibernate.hbm2ddl.auto">update</property>
		<property name="hibernate.show_sql">true</property>
		
		<!-- Mapping, After Hibernate (5.3+ / 6.x) mapping is not needed, as Hibernate can auto-detect entities on the classpath -->
        <!-- <mapping class="com.aaryan.model.Student"/> -->
        
    </session-factory>
</hibernate-configuration>